# BLOC — Training & Nutrition PWA

> A personal, offline-first Progressive Web App for structured weight training and nutrition tracking. Single HTML file. No backend. No dependencies.

![Version](https://img.shields.io/badge/version-v7.93-brightgreen) ![PWA](https://img.shields.io/badge/PWA-ready-blue) ![License](https://img.shields.io/badge/license-MIT-lightgrey)

---

## Overview

BLOC is a mobile-first PWA built entirely in a single `index.html` file — no build tools, no frameworks, no server. It is designed to be deployed on GitHub Pages and added to your home screen like a native app. All data is stored locally in `localStorage` and can be exported/imported as JSON at any time.

The app is structured around the concept of a **macrocycle** — a multi-week training block with defined goals, split type, and progression logic. Everything else (training logs, nutrition, body weight, steps, goals) lives within the context of an active macrocycle.

---

## Visual Design

### Typography
Inter is the sole typeface throughout, used for all display text, numbers, labels, and body content. The font is loaded from Google Fonts on first load and cached by the browser.

### Colour Palette
As of v7.58, the default dark palette uses a near-black "Signal" ground (`--bg: #161826`) with layered dark surface tokens and a muted purple accent (`--accent: #9184d9`). Light mode is toggled via `[data-mode="light"]` on the `<body>` element, which reverses surfaces and text into a light scheme. Accent and hero colours are intentionally left unchanged in both modes so each page's identity colour stays consistent.

Semantic colours — `--red`, `--warn`, `--amber`, `--blue`, `--purple`, `--ice-blue`, `--accent` — are used for progress indicators, schedule status pills, progression chips, and callout cards. `--warn` (a muted rose) was split out in v7.59–v7.60 to free up `--red` for its proper job: true red (`#E24B4A`) on danger/destructive states only.

### Dividers
Since v7.58, pages use a three-tier divider system instead of card wrappers: `.divider-hero` (heavy, under a hero card/section), `.divider` (medium, standard page-section divider), and `.divider-sm` (light, inter-row — e.g. table rows).

### Themes
As of v7.58, the app uses a single fixed palette (see Colour Palette above) rather than the earlier selectable multi-theme system — there is no longer a theme picker in Settings, and `data-theme`/per-page hero-colour overrides have been removed from the CSS. Dark/light mode (`setMode()`) is still available and still deliberately leaves the accent colour unchanged between modes.

### Hero Cards
As of v7.58's flat redesign, each page's hero section is no longer a bordered/gradient card — it's an unwrapped label/value block sitting directly on the page background, with callout chips and a progress bar where relevant. (Earlier versions used a gradient `.hero-card` per page; see Version History below.)

A small **Active Cycle status pill** appears in the corner of the hero card on applicable pages, showing the current cycle's schedule status with a solid heat-map colour.

### Nav Bar
As of the cardio session (~v7.66), the navigation bar spans the **full width** of the app shell (previously a floating auto-width pill), and every button's text label is **permanently visible** (previously only the active tab showed its label, others showed icon-only). It contains six buttons: Home, Progress, Plan, Train, Nutrition, Settings. (Body and Goals were folded into Settings and Plan respectively in v7.40–v7.52 — see below — and no longer have their own nav entries.)

Navigation icons are custom SVGs: Home=roof/house, Progress=bar chart, Plan=four squares, Train=barbell, Nutrition=fork and knife, Settings=cog.

A single `#nav-pill` highlight element is positioned and sized by `positionNavPill()` using `requestAnimationFrame`. It slides and morphs between buttons with a spring-curve CSS transition, producing a liquid bubble animation on every screen change. The pill colour is read from the active page's hero gradient via a temporary DOM probe, so the pill always matches the hero card exactly regardless of theme.

### Edge Fades
Gradient strips are pinned to the top and bottom of the app shell (matching `--app-height`). They fade scrolling content into the background colour so content is never hard-clipped at the safe-area boundary. They use `rgba(var(--bg-rgb), 0)` rather than `transparent` to avoid the grey-fade artifact in some browsers.

### Splash Screen (new in v7.88)
Plays on every fresh load of the app — an actual page parse (first open, a force-quit/reopen, or the OS evicting the page from background memory), not on resuming an already-open session that was simply backgrounded. A 2×2 grid of four blocks (Train/Fuel/Overcome/Repeat) drops in Tetris-style — bottom-left → bottom-right → top-right → top-left — each landing with a heavy thud (a hard compress with no bounce, plus a ground-shadow flash and dust kick-up either side), then flips to reveal its pillar icon. The blocks lift to reveal their pillar labels tucked underneath, then scatter outward. At the same moment, the BLOC wordmark rises to its resting position while the four pillar labels fly from their under-block position into a single tagline row beneath it, landing together — OVERCOME then picks up bold weight and the accent colour. See `TECHNICAL.md` §40 for the full sequence timing and implementation, including a measurement bug hit (and fixed) partway through development.

The real app underneath is hidden via a plain `visibility:hidden` CSS rule — not something toggled by JS after the fact — so there's no frame where it could flash into view before the splash covers the first paint. It's revealed at the exact moment the splash begins its own fade-out, producing a crossfade rather than a hard cut.

The frequency the splash plays at is gated by a single function, `qualifiesForSplash()`, currently hardcoded to play every time — written this way specifically so a different rule (e.g. once per calendar day) is a one-line change later.

---

## Screens

### Home (new in v7.31–v7.34; log boxes reworked in v7.53)
The default landing screen — a single-glance summary of the week plus inline log boxes, replacing Progress as the first tab.
- **Weekly hero card** — this week's (Mon–Sun) average kcal/protein/carbs/steps vs. the active goal's targets, each with an On track / Falling behind / Exceeding badge. Badge colour follows metric-specific polarity rather than a single rule: kcal is bad in both directions (under *or* over tolerance); protein and steps are only bad when under (exceeding is fine — more isn't penalised); carbs (and, by the same convention, fats elsewhere in the app) are only bad when over.
  - **Steps card — "View logs" button (v7.72)** — the steps metric row carries a permanent "View logs" button, tapping through to Settings → Body logs, regardless of whether steps are tracked yet this week. This replaced the old "Edit today's logs" link below the log boxes (see below). Earlier in the same pass, this button was mistakenly gated on `!tracked` and rendered on *every* untracked metric card (kcal/protein/carbs too) for a brand-new account with no data — fixed by keying it to `m.field === 'steps'` specifically instead of tracked-status
- **Count-up animation (v7.65)** — the four avg numbers and their progress bars count up from 0 together on an 800ms→1000ms eased timeline, but **only on an actual page load** (tapping into the Home tab, including app boot) — never on the in-page re-renders that follow a quick-log save, so logging your steps doesn't restart the animation on numbers you were just looking at. The same pattern (and the same fix for a CSS-transition/JS-animation fight on the bars — `transition:none` on the JS-driven fill) was applied to Progress, Train, and Nutrition's hero cards in the same pass; see those sections below.
- **On-track ring (fixed v7.90)** — the ring now counts metrics that are actually on-track (`withData - concerning`), not merely metrics with any data logged. Previously it could show e.g. "4/4" in the same render where the subline reported "Kcal off target this week" — two different questions (data presence vs. on-track status) sharing one ring. The subline's own "X of Y tracked so far" wording still uses the data-presence count, which is a genuinely different and still-correct thing to say there.
- **Per-card off-target advice (v7.90)** — a card whose badge is red gets its progress bar swapped to red and a red info icon top-right, replacing what used to be no on-card text at all (the reconciliation engine below existed since v7.65 but was never actually wired into the UI until this version). Tapping the icon opens a modal with that metric's full advice and a Dismiss button, keeping the card grid itself uncluttered either way.
- **Weekly advice — reconciled across kcal/protein/carbs/fats, full picture per card (v7.65; broadened v7.92; wording clarified v7.93)** — "Adjust your daily avg by ±X for the rest of the week (starting today/starting tomorrow) to hit target" on any day but the week's last, collapsing automatically into an exact "Hit X today" figure on the final day. This is not four independent per-metric calculations: if kcal or protein is flagged concerning, both are reconciled against each other and against carbs/fats first — protein's own catch-up rate is only honoured in full if it, plus carbs/fats sitting at their 50g/**20g** floors (fats floor raised from 15g in v7.92), still fits inside the kcal catch-up rate; if not, protein's shortfall is capped at 15g below its own flat daily goal and kcal absorbs the rest, landing above its own "ideal" rate for the week. Priority order is kcal, then protein, then carbs, then fats — **fats always takes the drop first**, with carbs only trimmed below its own independent rate once fats has already been squeezed to its 20g floor and there's still no room. As of v7.92, tapping **either** an off-target Kcal card or an off-target Protein card shows the **same full four-line picture** — Kcal, Protein, Carbs, and Fats together, each with a "New target" and "Projected weekly avg if met" bullet — rather than just that one card's own number, since raising protein always costs room that has to come from somewhere else. **As of v7.93**, the sentence itself states whether the adjusted rate applies "starting today" or "starting tomorrow" — the underlying calculation already decided this (today counts as settled the moment it has any logged data for that metric, shifting the remaining budget onto tomorrow), it just wasn't stated in the text before. A footnote states the priority order and clarifies none of this includes what's already logged today. Carbs only ever flags off-track when *over* its threshold, never when under (checked and confirmed correct in v7.92 — no code change needed there). This entire calculation is unchanged by, and completely independent from, Nutrition's own per-day suggested-values engine below. See `TECHNICAL.md` §30/§41/§43 for the full reconciliation algorithm, the card-modal wiring, and worked examples.
- **Upcoming goal banner** — appears only when the *next genuinely different* goal (compared by target values, not just the next chronologically-scheduled entry — a macrocycle's future weeks are often pre-scheduled as identical continuations that shouldn't count as "a new goal") starts within 6 days. Shows what's changing vs. the currently active goal (only the fields that actually differ), tapping through to Plan and briefly flashing that goal's row there
- **Inline log boxes (reworked in v7.53)** — reinstates the original Body-page "disappearing input" pattern rather than the icon-triggered mini-modals used briefly in v7.31–v7.52:
  - **Weight** and **steps** each show a plain input box every day; the box disappears the moment that field is logged for today, and reappears the next day. As of v7.65, steps gets the same save-confirmation swipe animation as weight/measurements — but with no result-flash text, so it just swipes and vanishes (see `TECHNICAL.md` §30)
  - **Measurements** works on its own cycle rather than a daily one — the waist/hip form only appears once **4 days** have passed since the last logged measurement (or immediately, if none have ever been logged), accompanied by a warning banner ("It has been 4 days since you last recorded your measurements."), and stays visible — not just for "today" — until a fresh measurement is logged, at which point the 4-day countdown restarts from that new date. The form itself (unit toggle between inches/cm, quarter-inch picker) is the same one previously behind a modal, now inlined directly into the screen
  - Boxes stack vertically rather than sitting side by side as icons, and only take up space when there's actually something to log
- **Planned food today** — today's planned recipes (by serving count) and non-recipe items (by grams), ordered by first meal appearance (Breakfast → Lunch → Dinner → Snacks), tapping through to Nutrition
- **Next session** — a single-row-per-exercise preview (name, sets × reps, suggested weight) of the next incomplete training session, titled with the actual session name, tapping through to Train

### Progress
- Displays data for the active macrocycle, with left/right arrows to cycle through past macrocycles
- **Count-up animation (v7.65)** — on an actual page load only (not on cycling between macrocycles, toggling the weight sparkline, or any other in-card action), the "Weight lost/gained so far" figure, the calendar-time progress bar (+ its % label), and the Volume/Steps/Kcal callouts all count up from 0 together on the same eased timeline as Home
- **Swipeable hero deck** — five slides with touch/mouse swipe and dot pagination:
  1. Cycle overview — name, goal, split, weeks remaining, progress bar
  2. Body weight — anchored to the live active cycle, with a day-index-aligned overlay comparing against past cycles at the same point in their timeline
  3. Volume — weekly training volume line chart
  4. Steps vs goal — bar chart for the current week
  5. Kcal vs goal — bar chart for the current week
- Tapping the hero deck opens the **Cycle History** modal, listing all past macrocycles with dates and goal types
- **Insights swipe deck** — a second, separate swipeable card stack directly below the hero (dot-paginated, no text labels under the dots): **Progress** (latest weight, 7-day average, weekly change, and — as of v7.28 — the trend/plateau signal card inline, right below the weight header, with the peak-loss/gain window callout pushed down below it), **Metabolism** (Mifflin BMR, log-based TDEE, calorie target, goal-weight ETA), and **Next cycle** (the recommendation engine described below)
- **Next cycle recommendation engine** — a fully deterministic recommendation for what to do once the active cycle ends, with an optional AI second opinion:
  - **Direction logic** — a cut recommends a maintenance bridge next; a bulk recommends a cut next (no bridge needed — a surplus doesn't cause the same adaptive suppression a deficit does); a maintenance bridge continues in whichever direction the cycle *before* it was heading (cut→bulk, bulk→cut), or offers a choice between the two if there's no history to determine one automatically. A short, mild cut (or a short bulk with a mild rate/surplus) instead recommends simply extending the current cycle — **and also computes the "normal logic" alternative it would have recommended otherwise**, shown as a small secondary option with its own "Build this instead" button, so extending is never a dead end
  - **Weekly kcal ramp** — a step-and-hold table (not a smooth curve) that continues from wherever the current cycle's plan actually left off, not recent adherence noise, stepping toward TDEE (the bridge case) or a computed steady-state target (a cut/bulk-direction case) a few weeks at a time, clamped to a computed safety floor
  - **Sustainable weight range** — a floor (for cuts) and ceiling (for bulks) derived from past maintenance cycles that held stable for 3+ weeks, with a conservative fallback when no such history exists yet
  - **Optional target weight / deadline** — checked against the sustainable range (hard boundary) and a 1.5%→1%/0.5% taper curve (pace boundary); an unsafe combination shows the shortfall plainly and offers two one-tap alternatives (nearest safe date for the stated weight, or nearest safe weight for the stated date)
  - **Maintenance TDEE-discrepancy flag** — mid-way through an active maintenance bridge, compares your actual recent intake/weight trend against the bridge's planned target and offers a one-tap recalibration of every not-yet-started goal if they've meaningfully diverged
  - **"Build this plan next"** — launches the same sequential goal-queue flow as the mid-cycle AI advice card below, with a macrocycle-creation step prepended
  - **BLOC second opinion** — within 3 weeks of the active cycle ending, an optional separate AI call reviews the same data (plus the engine's own floor/ceiling/taper/recalibration numbers) and either confirms the deterministic plan or proposes a refined one. Returns one plan normally; two when there's an unresolved target-weight/deadline conflict (one preserving each); or two — "sustainable" and "aggressive" — when no deadline was given for a cut or bulk (a bulk's aggressive variant only ever pushes kcal harder, never shortens the cycle — patience is the point). During a continuation, the call is framed around the "normal logic" alternative rather than the extension itself, with the model asked to explicitly weigh extending vs switching in its narrative. Daily step count is only ever varied as a lever during a cut; bulk and maintenance goals always get a flat 8,000 steps/day, applied client-side rather than requested from the model. Accepted plans route through the exact same goal-queue flow as the deterministic path
- Below the hero: body weight sparkline, weekly macro pie chart, weekly steps and kcal bar charts with today/7-day toggle
- **Goal period summary rows (v7.28)** — below the Statistics section, a compact row per goal period belonging to the selected cycle, ordered by start date, each showing its "Step N - label" name, date range, and every non-zero target. The goal covering today gets a green left accent; the next upcoming goal gets a red left accent plus a countdown subtitle — "Next up: starting in N days" while more than a week out, switching to "Next up: starting `<Weekday>`" from the Tuesday before it starts onward
- **Weekly tables — swipeable stack (v6.12; expanded to 3 cards in v7.28)** — a second swipe deck, dot-paginated the same way as the hero deck but with no text labels:
  1. **Weekly summary** — one row per calendar week showing average weight, weight delta, and average kcal/steps/protein. As of the cardio session, an **"Avg weight"** column sits immediately left of the existing "Δ weight" column. The weight delta compares **this week's average weight to the last week (with any data)'s average weight** — not a single day-to-day comparison — since two noisy daily readings can make a genuine plateau look like a steady loss (or vice versa)
  2. **Swings (new in v7.28)** — smallest/largest consecutive day-to-day change within each week, one column each for weight, kcal, steps, and protein (e.g. `−1.5lbs/+2.4lbs`), formatted with thousand-separators. Previously weight-only and embedded in the summary table; split out into its own slide so all four tracked metrics get the same volatility view
  3. **Measurements** — same week-bucket layout for waist/hip, only including weeks with at least one measurement logged; values shown are the most recent measurement logged that week, and deltas compare against the last known value from the most recent prior week with data (carried forward across empty weeks)
- **Trend insights card (v7.00; moved inside the Insights deck in v7.28)** — a goalType-branched deterministic signal card. Detects plateau (≥2 consecutive weeks of ≤0.5 lbs/week change on a loss cycle), caloric drift from baseline, undereating on a gain cycle, weight instability on maintenance. Shows a signal pill, headline, stat tiles, and a drift visualisation (baseline avg → recent avg). Baseline anchors on weeks 1–2 by default; slides forward to weeks 3–4 on loss cycles where early weeks show anomalously fast loss (>2% bodyweight/week), since glycogen/digestive clearance inflates those drops
- **AI advice card (v7.00)** — when a plateau or sustained imbalance is detected and an Anthropic API key is saved (Settings → AI Advice), an "Ask BLOC for advice" button appears. Tapping it calls `claude-sonnet-4-6` with your weekly data, safety floor (log-based BMR × 0.80), protein minimum (1g/lb bodyweight), a condensed history of every prior check-in this cycle (including any accepted revisions and why), and completed cycle rollup. The response is shown as a collapsible card with two recommendation paths (Sustainable and Aggressive), each with a sequence of goal periods. Choosing a path launches the **goal queue flow** — the current goal's end date is adjusted, upcoming goals for the cycle are cleared, and the recommended goals are opened for review and saving one by one. A 2-week cooldown prevents repeated calls
  - **Challenge this advice (v7.24)** — a single follow-up per advice response: "Challenge this →" swaps in a text box, and BLOC replies with an acknowledgment (admits a mistake or defends the original call), a fully revised Sustainable/Aggressive pair, and a diff view showing exactly which steps changed. Accept or decline before choosing a plan. If the revision is a minor correction (e.g. one field tightened), the original headline/reasoning stay put with the acknowledgment shown as a small note underneath; if BLOC substantially changes its read of the situation, the headline and reasoning are replaced outright rather than leaving a stale explanation next to a very different plan. Whichever way it goes, your next check-in this cycle is briefed on both the original and revised numbers so it never repeats advice you've already pushed back on

### Plan
- Create and manage **macrocycles** with:
  - Name, start date, goal type (Weight Loss / Strength Gain / Maintenance)
  - **Start date must be a Monday (v7.28)** — enforced on both creation and edits; a non-Monday date blocks the save with a flashed red field and an inline error, since every deload-week, mesocycle-date, and calendar-week calculation in the app assumes week 1 starts on one. The suggested default start date is automatically snapped forward to the next Monday
  - Split type: **PPL** (Push/Pull/Legs), **Full Body**, or **Custom** (freely named sessions)
  - Number of mesocycles (weeks), optional microcycles (alternating A/B sessions within a week)
  - Optional target body weight, a configurable weight increment per mesocycle (available for any goal type, not just weight-loss cycles)
  - **Sessions per week is calculated automatically** from whichever split you build below it, live-updating as you add/remove sessions — not a separate number to keep in sync yourself
- **Extend macrocycle (v7.91)** — grows an in-progress macrocycle by a chosen number of calendar weeks without touching its original mesocycle count, so nothing about already-completed weeks' set-count spacing gets retroactively reflowed. The extra weeks repeat the final mesocycle's exercises at peak sets (there's only ever one exercise template per macro — mesocycle 1's — reused algorithmically throughout, so "repeating" it is just holding sets flat while weight/rep progression keeps running uninterrupted). An "Extend" button sits below "+ New" on the macrocycle hero (numeric-keypad weeks input); once set, an extension badge appears below the hero showing "Extended by Nw", replacing the Extend button until it's removed. Tapping the badge opens edit (grow/shrink) or remove. If your mesocycle length doesn't divide evenly into the requested weeks (e.g. 2-week mesocycles + a 3-week extension), the trailing partial mesocycle only gets its M1 microcycle — M2 is dropped for that one week, both in Train's session list and its day-tab picker. Confirming the extension immediately prompts a choice: **extend your last goal period** to the new cycle end date, or **add a new goal period** for just the extra weeks, pre-filled from your last goal's targets (start date the day after it, same kcal/steps/macro split, adjustable before saving). The Anthropic advice prompts pick up the new end date automatically. See `TECHNICAL.md` §42 for the full mechanism.
- Sessions (Push/Pull/Legs/custom) are shown as individually **collapsible, renameable cards** — tap the header to expand/collapse, tap the name to rename in place. The header always shows a live summary: exercise count, total sets, and total week-1 volume across every exercise in that session (including supersets)
  - **Auto-collapse (v7.40)** — a session with at least one exercise defined starts collapsed each time you arrive at the Plan tab; an empty session stays expanded (nothing to hide yet). Manually toggling a session, or opening its add/edit-exercise modal, overrides the default until you next navigate away from Plan and back, at which point the override resets and the default rule reapplies
- Per-exercise configuration: name, sets, reps, starting weight, exercise type (**Standard**, **Giant Set**, **Pause Set**, **Drop Set**), heavy leg flag, per-side vs total-on-bar tracking, minimum weight increment
  - **Weight / Cardio category (new in the cardio session, ~v7.60–v7.68)** — a toggle at the top of the exercise modal reconfigures the whole form. **Cardio** exercises (Run, Bike, Row built in, plus any custom cardio exercise) drop the weight/reps/type fields in favour of: a **Time vs Distance** target toggle, the corresponding time (min/sec) or distance input, a per-exercise km/m unit picker (only shown when the global Distance Units preference — see Settings below — is set to Km), and optional speed/resistance level fields. Legacy exercises with no stored category default to Weight via `getLibraryCategory()` — no migration was needed
  - **Sets range widened to 1–15 (was 2–7)**, dropping the separate "standalone" checkbox that used to exist purely to allow a 1-set session — a single-set session is now just a 1→1 sets range, for both weight and cardio exercises
  - **Exercise picker filters to the active category** — the Weight and Cardio exercise lists never mix; a custom exercise appears under whichever category it was created in. The "Add Custom Exercise" modal hides the Body Part field when adding a cardio exercise, since it doesn't apply
  - **Drop Set** exercises plan a starting weight *and* (as of v6.09) a reps target for the main set, same as any other type — only the drop portion has no target and is logged to failure live, starting in week 1
  - **Last logged reference** — when adding or editing an exercise, a note below the exercise name shows what you actually logged last time for that exercise (per set type, if you've trained it more than one way — e.g. separate lines for a standard-set history and a giant-set history), and new exercises prefill their reps/weight/tracking-mode defaults from it automatically. Purely a planning aid — never affects any calculation, and always overridable
- **Cardio exercises on the Plan card** — shown with a readable summary (e.g. "3–5 sets · 30s target · resistance 8") and a cardio badge instead of the usual weight/reps summary; a cardio member of a superset shows "follows leader" in place of its own (irrelevant) sets range
- **Supersets** — link two or more exercises into a group with a shared badge and optional custom name; reorder within or across a group by drag; unlink an individual exercise or the whole group back into standalone exercises. Drop sets can't be added to or linked into a superset. Cardio exercises *can* be superset members alongside weight exercises, following the leader's set count
- Drag-to-reorder exercises, including moving in/out of superset groups — this ordering drives the exercise order on the Train page
- Edit, copy, and delete macrocycles — tap the macrocycle card to edit; swipe left to reveal copy and delete buttons
- Body weight progress bar within the plan card, showing start → current → target
- **Weeks 2+ progression preview** — collapsible, grouped by session (not by week): expanding a session shows every exercise's sets/reps/weight target broken down week-by-week across the whole cycle
- **Body part volume table** — below the progression preview, one row per body part (resolved by looking up each exercise's name against the exercise library), showing the cycle's total minimum and maximum training volume for that body part, sorted by minimum descending. Min/max reflects whichever theoretical progression path (all-weight vs all-reps) produces less/more total volume; pause sets are weight-only either way, so their min and max are identical. Drop sets contribute their main set's projected volume like any other exercise; only the drop portion doesn't contribute, since it has no plan-time target to project from
- Schedule-status pills with solid heat-map colours indicating where each session falls in the cycle

### Goals (moved into Plan in v7.40 — no longer its own screen)
- Below the macrocycle hero card, a **Goals section** lists every goal period belonging to the current macro, in the same row style Progress's cycle-goals list uses — a "+ Build goals" button opens the same goal modal that used to live on the standalone Goals screen. The section is collapsible (expanded by default), each row taps to edit and swipes to delete
- **Dismissible create-goal prompt** — saving or editing a macrocycle with no goal linked yet offers to add one via a Cancel/Add-goal confirm dialog; declining is a no-op, not a blocker
- **Delete button** added directly to the goal modal itself (previously deletion was swipe-only from the Goals list)
- Set daily targets per macrocycle: steps, kcal, protein, carbs, fats. Kcal and steps are required — the input border flashes red if you try to save without them
- Goal periods **cannot overlap** — a new goal defaults to starting the day after the latest existing goal ends (across every macrocycle), and saving is blocked with an inline error if the dates collide with any existing goal
- Macro sliders (protein g/lb multiplier, carb/fat split) back-derive their starting position from an existing goal's saved grams when editing. For a goal that's already **active or past**, saving preserves the exact macros already stored unless you explicitly drag a slider — it won't silently drift just because your bodyweight has changed since the goal was created. New/upcoming goals always compute live from current bodyweight
- Progress's own goal-period summary rows (see below) are now restricted to active + upcoming only — full history, including past goals, lives here on Plan

### Train
- Week and day selector to navigate the macrocycle — day-tab pills are grouped **one row per real calendar week**, so a 2-week mesocycle shows a separate row for each microcycle, while a 1-week mesocycle (even one that still uses microcycles) stays on a single row
- **Count-up animation (v7.65)** — "Session progress" % and its bar count up from 0 together on an actual Train page load only — never on the re-renders that follow logging, toggling, or clearing a set, so saving a set doesn't restart the animation
- **Calendar week dates on the hero (v7.28)** — the hero card shows the real date range (e.g. `28 Jul – 3 Aug`) for whichever mesocycle + microcycle is currently selected, fanned out from the macrocycle's start date — a forward-planning aid for lining deload weeks up against upcoming goal periods without counting weeks by hand
- **Deload weeks** — mark the currently-selected week (or, for a 2-week mesocycle, the specific microcycle you're viewing) as a deload via a toggle on the hero card. All weights for that week drop to 60% of last logged, rounded to each exercise's own increment; reps stay the same. The session immediately following a deload automatically reverts to your last genuine numbers with progression suggestions hidden, so one deload never permanently resets your baseline — normal progression resumes the week after that. The hero also shows how many weeks it's been since your last deload, once you've done one
- Per-session exercise cards showing:
  - A "Last wk: ↑ weight" / "↑ reps" badge in the header, inferred from whichever explicitly-chosen progression path was used last week — or, if none was explicitly chosen, backfilled by comparing last week's actual weight/reps against the week before; shows "no progression" if neither increased. Replaced by a "deload" badge during deload weeks, and hidden during the week right after one
  - Last week's logged sets (weight × reps) shown directly in each set row, replacing what used to be a redundant repeat of the current week's suggested value
  - Suggested progression for the current week (weight or reps), with a progression chip showing the delta. **Each set gets its own suggestion** (v6.09), carried forward from that same set number the last time you trained this exercise — so if you had to drop the weight on a later set in a session, that adjustment carries into next mesocycle's suggestion for that set specifically, rather than every set inheriting whatever happened on set 1. A newly-added set (from a plan that ramps sets up over the cycle) simply inherits the previous week's final set's suggestion
  - Set logging with weight, reps, and a done toggle per set
  - **Fill Suggested** button — fills every set with its own suggested values in one tap, with an amber flash animation
  - Superset cards show the same last-week badge and progression data per member, plus each member's exercise type badge, consolidated into a compact per-exercise summary line rather than a full per-set breakdown. Drop sets can't be superset members
  - **Drop Set** exercises expand into one block per set, each containing a Main row and a Drop row, sharing one done-checkbox — a set isn't complete until both halves are logged. The main set now has a real reps target like any other exercise type (v6.09); only the drop row shows "to failure", since the drop is always a reduction in weight taken to failure and its reps are only ever discovered live
- **Cardio exercises (new in the cardio session, ~v7.60–v7.68)** — a full logging card per cardio exercise, separate from the weight-exercise set-row layout:
  - Expand/collapse, with column headers (Set / Target — whichever of time or distance was fixed at plan time / logged unit / speed / resistance / ✓) laid out on a CSS grid (matching the technique weight exercises already used — fixes an early misalignment bug between the header and its data rows)
  - One row per set, logging whichever metric *isn't* the fixed plan-time target (e.g. if distance is the target, you log time; if time is the target, you log distance), plus optional speed/resistance level inputs and a done toggle
  - Every input's placeholder is last week's logged value at that same set index — there's no computed suggestion, since cardio has no progression
  - The header shows the last **logged** speed/resistance (not just the plan default) and a "Last wk: +500m / −30s"-style delta badge
  - **Fill Suggested** only fills the speed/resistance level fields (there's nothing else to suggest); **Clear** wipes the exercise's logs as normal
  - Fully bypassed from progression suggestions, the Progression Compliance Guard, and deload effects — cardio never has a "suggested" number to comply with or deload
  - Can be a superset member alongside weight exercises, following the leader's set count; each member still gets its own per-row mini labels, since different members can have different metric/level configurations
- **Exercise history** — completing a set (via the checkbox or the one-tap fill-and-complete shortcut) remembers your weight/reps/tracking-mode for that exercise and set type, which is what powers the Last Logged reference and defaults on the Plan page. Deload-week completions are deliberately never recorded, so a reduced week can't skew future suggestions. This always reads from set 1 specifically, regardless of which set you interacted with or which sets got filled by a quick-fill shortcut — a later set's adjusted numbers can never override what set 1's own suggestion should be next time
- **Quick fill + complete** — a one-tap shortcut on the collapsed exercise card that fills every set with its suggested values and marks them all done. Drop sets get the same shortcut (v6.10) — previously there was no quick-complete option for them at all, since there was no main-set reps target to suggest; it now fills weight, reps, drop weight, and drop reps, same as any other exercise type
- **Rest Timer** (clock icon, top-right):
  - **Countdown** mode with an iOS-style scroll drum picker (0–59 min, 0–59 sec), defaulting to 1:00. Digits turn amber in the final 10 seconds. Three-beep audio alert on completion via the Web Audio API — does not interrupt music or podcast playback
  - **Stopwatch** mode with tenths-of-second precision
  - Closing the modal cancels and resets all timers
  - Clock icon turns accent green while a timer is running

### Body (folded into Settings in v7.51 — no longer its own screen)
Reached via Settings → Profile card → **Body logs**, which opens a modal containing just the grouped log list described below (the weight chart, inline "log weigh-in" card, and measurement reminder that used to sit above it were all removed in v7.51 along with the computation that fed them — that data is now only ever entered via the log-entry modal itself, not a standing form). Home's steps card carries a permanent **"View logs"** button (v7.72) that deep-links straight to this same modal via `openSettingsBodyLogs()`.
- Log body weight (lbs) and steps per day, tap an entry to edit, swipe left to reveal delete
- **Waist/hip measurements (v6.12)** — optional entries logged alongside weight/steps in the same edit modal. Input unit toggles between inches (whole number + a quarter-inch picker — 0/¼/½/¾) and centimetres (single decimal field); everything is stored internally in inches, so switching units later never corrupts historical entries
- Gender/height/birthday (driving the BMR formula) now live in their own **About me** modal, opened directly from Settings → Profile, separate from the log list
- BMR/TDEE (Mifflin-St Jeor) calculated from profile + activity level, with activity level derived from your all-time average logged steps (intentionally unscoped — more historical data makes the estimate more accurate, not less)
- **Monthly grouping (v6.11)** — any month collapses into a single tappable "Mmm YYYY" group once its entries have both weight and steps filled in, showing entry count and average weight; tap to expand. Entries missing either field always stay visible individually, regardless of month — so if your steps take a day to sync, that entry won't get buried inside a collapsed group right when you're most likely to want to finish filling it in. Groups sort newest month first

### Nutrition
- Per-day food logging across named meals (Breakfast, Lunch, Dinner, Snacks)
- Hero card: tap to open a date picker, or swipe left/right to step one day at a time. A quick-add button in the corner lets you log the day's totals directly (kcal/protein/carbs/fats), overriding whatever's in the meal logs below for that day's totals
- **Count-up animation (v7.65)** — the kcal figure and its progress bar, plus the Protein/Carbs/Fats callouts, count up from 0 together on an actual Nutrition page load only — never on the re-renders that follow logging food, quick-adding, or switching the selected date
- **Adjusted to get the week back on track — per-day planning row (v7.93)** — a second row of four tiles (Kcal/Protein/Carbs/Fats, "logged / suggested" — same format as the goal callouts above it) appears below the day's goals, shown from **today through the end of the current calendar week (Sunday)** — never on a day that's already passed, and never in a past or future week — and only when kcal or protein is flagged concerning this week (using Home's real, today-anchored pace check to decide whether the week needs correcting at all). The **numbers themselves** are a day-specific calculation, entirely separate from Home's own week-pace engine: whichever day you're viewing (today, or a day later this week you're planning ahead), its suggested figure is based only on what's logged on days *strictly before* it — never its own logged data, however much of it there already is. This is what makes day-by-day planning work: adjusting today's own log never moves today's own suggested figure, only tomorrow's (and every day after) — so you can plan the whole week one day at a time, in order, without a later day's plan affecting an earlier day's numbers, and without today's own in-progress logging retroactively changing its own target throughout the day. Uses the identical kcal > protein > carbs > fats priority cascade as Home's engine (fats floor 20g, carbs floor 50g, protein capped 15g under goal), just computed against each day's own cutoff instead of today's. See `TECHNICAL.md` §44
- Food lookup via:
  - **Scan Barcode** — live camera scanning using the native `BarcodeDetector` API (Safari 17+, Chrome 83+) with automatic ZXing JS fallback. Camera launches immediately on tap. Manual barcode entry available below the viewfinder as a fallback. Looks up the scanned code against the Open Food Facts API
  - **Manual entry** — a **Per item / Per gram** toggle (new in v7.65). Per item is the original behaviour — a name (optional; defaults to "Manual entry") and direct macros, logged once as a flat total, never saved to the food library. Per gram is for something the scanner couldn't find that's genuinely weighed (e.g. a new product's mince) — you enter the totals for however much you're using (not a per-100g rate), and the app divides by the grams you gave it once and stores that as a real per-gram rate, exactly like a barcode scan. It's then editable by weight afterwards (grams changes → macros rescale automatically) and gets saved to the food library so it's reusable next time
  - **Recipe** — pick a saved recipe from the food library
  - **Food library** — search previously used foods, sorted by most recently logged
- Serving confirmation modal showing grams and servings fields, live macro preview, and (for recipes) a full ingredient breakdown. This modal (and the food library editor) use a taller sheet whose internal list independently shrinks to stay above the keyboard, rather than the sheet itself resizing
- Swipe-to-copy from yesterday — an animated swipe strip under each meal shows the previous day's items and copies them on a one-third-width swipe right
- Meal ellipsis menu (`···`) — copy or move an entire meal to any date and meal target, or save the whole meal as a new recipe (this only creates the recipe from the meal's current ingredients — it does not automatically log that recipe anywhere)
- Per-item interactions — tap a food entry to edit it; swipe left to reveal copy and delete buttons. Small print shown per item (brand, serving size) varies by source: recipes show serving count, everything else shows brand where known
- Daily macro panel and daily kcal card with progress vs. the active goal
- 7-day weekly tab with bar charts and macro pie chart
- Personal **food library** — foods saved automatically on first entry; recipes stored with brand "My Recipe"; Per gram manual entries (v7.65) save here too, tagged `source: 'manual-weight'`
- **Recipe builder** — multi-step: name and servings, then add ingredients by barcode scan, manual entry (also gained the **Per item / Per gram** toggle in v7.65 — same mechanics as above), or (v6.12) **Food Library search** — the same search-and-select modal used for regular meal logging, reused in an ingredient-adding context: the Recipes/Manual/Scan Barcode action row is hidden, quick-add and tap-to-edit-serving both add straight to the recipe instead of logging to a meal, and the search list stays open (or reopens) after each addition so several ingredients can be added in a row. Reachable from the food library ("My Recipes" in Settings) or from a meal's ellipsis menu
  - **Ingredient gram editing fixed (v7.65)** — previously, adding a library/scanned food into a recipe silently discarded its real gram amount and per-gram rate, always storing `grams: 1`; and any ingredient predating the per-gram schema fields (pre-v6.x recipes) couldn't be edited by weight at all — its real grams (e.g. "200g chicken") got misread as a servings count, corrupting the edit view into nonsense like "200 servings, 1 kcal each". Both are fixed: library-sourced ingredients now keep their real weight and rate, and any ingredient with no rate but a real grams figure has one safely derived (`kcal ÷ grams`) the moment you edit it — no migration script, no data loss, works retroactively on recipes saved years ago. True manual "per item" ingredients (e.g. "2 sausages") and a nested recipe used as an ingredient are unaffected — those are genuinely servings-based, not a bug. See `TECHNICAL.md` §14 for the full mechanism.
- **Sample Day Library (new in v7.25)** — save a whole day of logging for reuse later, and pull it back onto a new date in one tap:
  - **Save prompt** — appears below the hero on any day whose totals land close to that day's own goal targets and has a recipe logged for Dinner: "On target for your goal — save this day to pre-fill later?" Tapping it stores every meal from that day into a library, grouped by goal. Once saved, the prompt is replaced by a "✓ Saved to library" pill inside the hero card itself
  - **Fill Day** — next to Quick Add in the hero's corner, opens a picker of previously saved days for the current goal (or any goal with sufficiently similar targets, even one that's never had a day saved under it) and copies every meal from the chosen day onto the current date, with a confirm-to-overwrite warning if the date already has logs
  - **Dinner-entry prompt (new in v7.53)** — logging anything into Dinner, on any date whose goal already has a matching saved library, triggers a "There is an approved day already saved for this meal and this goal — want to fill the whole day?" prompt. Accepting fills the entire day from the most recently saved match — the same mechanism Fill Day itself uses, so it overwrites everything logged for that date, including the item just added. Declining just closes the prompt and leaves the entry as logged. This checks on every item added to Dinner, not just the first
  - **Goal linking** — a saved day's library is scoped to whichever goal created it, then automatically grows to include any other goal (now, or discovered later) whose kcal/protein/carbs/fats land close enough to count as "the same kind of day" — so a similar goal weeks later can immediately draw on an older goal's saved library
  - **Settings → Nutrition → Sample libraries** — a screen (reached only from Settings) listing every saved library: a bold pill for the goal that originated it, subtler pills for every other goal currently linked to it, and its saved days as swipeable rows (tap to relabel, swipe to delete)

### Goals
See **Goals**, now under the **Plan** section above — Goals was folded into Plan in v7.40 and is no longer its own screen or nav-bar entry.

### Settings (redesigned across v7.50–v7.52)
A list of collapsible cards, each expanded by default except Danger Zone:
- **About this app** — tappable row (name + version badge); opens a modal showing storage usage
- **Profile** card — App preferences (Dark/Light mode + the nine theme swatches, moved here from their own top-level section; **Distance Units** — Km/Metres vs Miles, new in the cardio session, governing the per-exercise km/m picker on cardio exercises in Plan), About me (gender/height/birthday for the BMR formula), Body logs (the grouped log list described above), Linked services → API Key (the optional Anthropic key that powers "Ask BLOC for advice" — moved here from an old standalone "AI Advice" section; still stored separately in `localStorage`, still never included in JSON exports)
- **Nutrition** card — My Recipes (accent-tinted, first), Sample libraries (the Sample Day Library management screen described above), View/edit food library, Export food library, Import food library, Import recipe
- **Exercise** card — View/edit library, Export, Import
- **Backup** — Export JSON backup, always visible, not behind a button
- **Danger Zone** card — **collapsed by default**, unlike every other card on the page, since these are the two genuinely destructive actions: Import JSON backup (replaces all current data) and Clear all data (wipes everything but preserves theme/mode, since the confirm dialog only ever promises to delete tracked data). Clear all data keeps its solid red fill; restore-from-backup keeps red text on the app's normal button style, since it's recoverable by re-importing your last export
- Every card's buttons are visually identical (icon + label, no descriptive text, no nested cards) — the two-tier primary/secondary button hierarchy from v6.10 was dropped in this redesign in favour of one consistent button style throughout
- **Modal stacking** — buttons that open their own modal (App preferences, About me, Body logs, Linked services → API key, Sample libraries → a saved day's editor) stack on top of whatever's already open rather than closing it, so backing out returns you to exactly where you were. The three buttons that lead into the pre-existing Food Library, My Recipes, and Exercise Library editors are the deliberate exception — those have their own substantial modal chains built around a fixed stacking order, so those three still close the Settings screen's card view first rather than layering on top of it

---

## Barcode Scanning

BLOC uses a two-tier detection strategy, chosen automatically at runtime:

1. **Native `BarcodeDetector`** (Safari 17+, Chrome 83+) — zero-overhead, runs on-device
2. **ZXing JS** (bundled, ~336KB) — pure-JS fallback for older browsers

The camera launches immediately when "Scan Barcode" is tapped. On detection the viewfinder corners flash white, the device vibrates, and the barcode is looked up against Open Food Facts automatically. A manual entry field below the viewfinder serves as a final fallback. The camera stops as soon as a product is found or the modal is closed.

---

## Progression Logic

BLOC automatically calculates suggested weights and reps week-over-week based on the macrocycle goal type. The weight increment is user-configurable per macrocycle for any goal type (defaults to 2.5 kg); the heavy-leg increments are fixed:

| Goal type | Standard exercises | Heavy leg exercises |
|---|---|---|
| Weight Loss | + your configured increment / mesocycle | +5 kg / mesocycle |
| Strength Gain | + your configured increment / mesocycle | +10 kg / mesocycle |

For rep progression, 1 rep is added per week to each set.

**Exercise types:**
- **Standard** — weight or rep progression, chosen per exercise per week
- **Giant Set** — weight or rep progression (giant-set reps add +10/week when reps is chosen)
- **Pause Set** — fixed reps, weight progression only
- **Drop Set** — a real reps target is planned for the main set, same as any other type (added in v6.09; previously no rep target was ever planned). Only the drop portion has no target and is logged to failure live, starting in week 1. Once there's a prior week to compare against, progression applies identically to both, and — since v6.09 — is suggested per set rather than uniformly from set 1
- **Cardio** (new in the cardio session, ~v7.60–v7.68) — no progression whatsoever. Placeholders on Train are last week's logged values, never a computed suggestion; cardio is fully exempt from the Progression Compliance Guard and from deload weight reduction

**Per-set suggestions (v6.09):** each set's suggested weight/reps is now carried forward from that same set number the last time you trained the exercise, rather than every set copying whatever set 1 did. So if you had to drop the weight partway through a session, that adjustment carries forward correctly into the next mesocycle for that specific set — the other sets still progress normally from their own numbers. A newly-added set (from a plan that adds sets partway through the cycle) inherits the previous week's final set's suggestion.

**Deload weeks** temporarily override all of this: every exercise's weight drops to 60% of last logged (rounded to that exercise's own increment), reps stay the same, and progression suggestions are hidden for the deload week itself and the single session right after it — which instead reverts to your last genuine numbers so the deload never permanently resets your baseline.

This same weight-jump formula is shared by the Plan page's progression preview and the Train page's live recommendations, so both always agree.

---

## Architecture

| Concern | Approach |
|---|---|
| File structure | Single `index.html` — HTML, CSS, and JS in one file, one main `<script>` block (plus a separate `<script>` tag holding the bundled ZXing library) |
| Storage | `localStorage` (`bloc_state` key) |
| Fonts | Google Fonts — Inter (all weights) |
| Food data | Open Food Facts API (barcode lookup) |
| Barcode scanning | Native `BarcodeDetector` API → ZXing JS bundle (bundled, ~336KB inline) |
| Audio | Web Audio API — sine wave oscillators, no audio files |
| PWA | `apple-mobile-web-app-capable`, `viewport-fit=cover` meta tags; add to home screen via Safari/Chrome share sheet |
| Boot sequence | Splash screen (v7.88) plays on every fresh load, ahead of `#app` becoming visible — see Visual Design → Splash Screen above and `TECHNICAL.md` §40. `qualifiesForSplash()` is a single swappable gate, currently always `true` |
| iOS viewport | `measureEnv()` DOM probe reads actual safe-area inset values; `--app-height` CSS var set via `window.visualViewport.height`; `#nav` positioned as `absolute` inside `#app` rather than `position: fixed` |
| Keyboard type per field (v7.54) | `inputmode="numeric"` (whole numbers: steps, reps, waist/hip whole-inch pickers, barcode entry, height ft/in, mesocycle count) or `inputmode="decimal"` (weight, kcal/macros, grams/servings, cm measurements) set field-by-field after a full audit of every input in the app — independent of each field's `type` attribute, so existing validation is untouched |

No service worker is registered, meaning the app requires an internet connection on first load for fonts. All app logic and data is fully offline after that.

---

## Deployment

The app is designed to be deployed as a GitHub Pages site from a single `index.html` at the repo root.

1. Fork or clone this repo
2. Push `index.html` to the `main` branch
3. Enable GitHub Pages in repo Settings → Pages → Source: `main` / `/ (root)`
4. Visit your Pages URL and use your browser's **Add to Home Screen** option to install as a PWA

---

## Data & Backup

All data lives in your browser's `localStorage`. It is not synced to any server. A backup includes everything — macrocycles, exercises, training logs, deload markers, exercise history, nutrition, body weight, and goals.

**It is strongly recommended to export a JSON backup regularly** via Settings → Export JSON backup, and store it in a safe location (e.g. iCloud Files, Google Drive).

To restore: Settings → Danger Zone → Import JSON backup → select your `.json` file.

---

## Known Limitations

- No service worker — fonts require an initial network request
- Data is device-local; no cross-device sync
- No native push notifications for timer alerts when the app is backgrounded
- `localStorage` is capped at 5–10 MB; extremely large food libraries or years of logs could approach this
- Body weight is tracked in lbs while training weight is tracked in kg — an intentional, but non-obvious, split (there's no unit toggle for body weight itself). Waist/hip measurements (v6.12), by contrast, do have an inches/cm input toggle — internally always stored in inches
- Drop Set exercises' main set now contributes to the Plan page's *theoretical* volume projections (Week-1 session summary, body-part volume table) like any other exercise, as of v6.09 — only the drop portion still doesn't, since it has no plan-time target to project from. Real logged volume was never affected either way
- Sample Day Library: once a goal formally links to a library (by having a day saved under it), that link is permanent — there's no path that removes it later even if the goal's targets are subsequently edited to fall well outside the library's tolerance band. See `TECHNICAL.md` §28/§29 for the full detail, plus the `Infinity`/JSON storage gotcha (fixed v7.30) that's worth reading before extending this feature
- Progression Compliance Guard (v7.55–v7.57) locks a whole exercise if *any* set misses its target that week, not just the specific set(s) that missed — a deliberate simplification rather than tracking per-set lock state. See `TECHNICAL.md` §12 for the full behaviour
- Recipe ingredients added as true "Per item" manual entries (before v7.65's "Per gram" mode existed, every manual ingredient was necessarily this type) have no recoverable weight — if one was actually meant to be tracked by grams (e.g. a weighed food the scanner couldn't find), it has to be recreated via "Per gram" rather than converted in place, since the original entry never stored a real gram amount to begin with. Ingredients from *before* the per-gram schema fields existed at all (pre-v6.x recipes) are unaffected by this — those get a per-gram rate safely derived from their existing totals the moment they're edited, no recreation needed
- Splash screen (v7.88): timing constants (drop stagger, thud/lift/rise durations, lift and rise distances) were tuned by eye against an isolated demo page during development, not measured against the real app's actual boot performance (state load, `measureAll()`, font loading, etc. all running concurrently) or covered by any automated test. Worth a real-device pass to confirm the pacing still reads well once it's competing with the app's own boot work. See `TECHNICAL.md` §40

---

## Version History

Recent versions (v5.26 onward) reflect a page-by-page legacy audit pass — removing dead code left over from earlier redesigns, fixing bugs found along the way, and adding a handful of features that came up during the audit. Selected highlights below; see in-file version badge for the exact current build.

| Version | Notes |
|---|---|
| v7.93 | **New: Nutrition per-day planning row + several fixes.** Nutrition gains an "Adjusted to get the week back on track" row (Kcal/Protein/Carbs/Fats, "logged / suggested") shown from today through the end of the calendar week, only when the week is currently flagged concerning. Its numbers come from a brand-new day-specific calculation (`getDayViewRequiredDaily`/`getNutrDayReconciledAdvice`) — deliberately separate from Home's own pacing engine — where each day's suggestion depends only on days strictly *before* it, so planning a day never moves its own number, only the next day's. Home's advice text now also states whether an adjusted rate applies "starting today" or "starting tomorrow" (wording only — the underlying calculation is unchanged). Bug fixes: a duplicated divider below the macrocycle extension badge on Plan (the badge and the Goals section were each drawing their own); tap-outside-to-close was silently missing on four modals introduced in v7.90/v7.91 (metric advice, and all three Extend-macrocycle modals) since they lacked the `.modal-sheet` wrapper `initModal()` requires — added explicit backdrop handlers to each. See `TECHNICAL.md` §44 |
| v7.92 | **Home advice — full reconciled picture per card, fats floor raised to 20g.** `RECONCILE_FATS_FLOOR` 15g → 20g (priority order kcal > protein > carbs > fats unchanged — fats always drops first, carbs only once fats is at its floor). Tapping an off-target Kcal or Protein card now shows the full four-line reconciled picture (Kcal/Protein/Carbs/Fats together, byte-identical from either card) instead of just that card's own number, since raising protein always costs room that has to come from elsewhere. Carbs' "only flags off-track when over threshold, never under" behaviour checked and confirmed already correct — no change needed. See `TECHNICAL.md` §43 |
| v7.91 | **New: Extend Macrocycle.** Grows an in-progress macrocycle by N calendar weeks via a new `macro.extensionWeeks` field, kept entirely separate from `macro.weeks` so `getWeekSets`'s interpolation denominator — and therefore every already-completed week's set-count spacing — is never retroactively touched. Extension weeks repeat the final mesocycle's exercises at peak sets (`getWeekSets` now returns `ex.setsEnd` flat for any week beyond the original count) while weight/rep progression keeps running unaffected. Handles a mesocycle length that doesn't divide evenly into the requested weeks by dropping the trailing partial mesocycle's M2 microcycle. New Extend button (hero, below "+ New") → numeric weeks input → extend-last-goal-or-add-new-goal choice, plus an edit/remove badge once an extension exists. AI advice prompts' duration/end-date lines are extension-aware automatically. See `TECHNICAL.md` §42 |
| v7.90 | **Home — on-track ring fix + per-card off-target advice.** The hero ring previously counted metrics with any data logged this week, not metrics actually on track — could show "4/4" in the same render as a "Kcal off target" subline. Now counts on-track metrics directly. Off-target cards get a red progress bar + red info icon (replacing on-card text entirely) that opens a modal with that metric's advice and a Dismiss button — wiring up the reconciliation engine built in v7.65, which had never actually been called from anywhere until now. See `TECHNICAL.md` §41 |
| v7.88 | **New: Splash screen.** Plays on every fresh load (a real page parse — first open, force-quit/reopen, or the OS evicting the app from background memory — never on resuming an already-open, merely-backgrounded session). Four pillar blocks (Train/Fuel/Overcome/Repeat) drop in Tetris order — bottom-left → bottom-right → top-right → top-left — each landing with a heavy thud (hard compress, no bounce, plus a ground-shadow flash and dust kick-up), flip to reveal their pillar icon, lift to reveal their pillar label tucked underneath, then scatter. At the same moment, the BLOC wordmark rises to its resting position while the four pillar labels fly from their under-block position into a single tagline row beneath it, landing together on the same timing; OVERCOME then picks up bold weight and the accent colour. The real app stays hidden (plain CSS `visibility:hidden`, so there's no flash of it before the splash covers the first paint) until the splash's own fade-out, at which point it's revealed at the same instant for a crossfade rather than a hard cut. `qualifiesForSplash()` is a single, swappable gate — currently always plays; a different frequency (e.g. once per calendar day) is a one-line change. See `TECHNICAL.md` §40, including a measurement-timing bug hit and fixed partway through development |
| v7.87 | **Onboarding tour engine — dynamic top/bottom tooltip placement restored, this time with the safe-area clamp it was always missing.** Follow-up to v7.86 after clarifying the actual ask: the tooltip should be able to render either above or below the target (whichever side has more room), it just must never be allowed to render above the safe area — not locked to always sit directly below it. Reverted to the original "which side has more room" placement decision (`roomBelow >= roomAbove ? 'bottom' : 'top'`, comparing actual available space on each side rather than v7.84's "prefer below unless it doesn't fit"), and restored the v7.84 scroll behavior (`scrollIntoView({block:'center'})` for normal targets, an explicit top-margin offset scroll for targets taller than 85% of the viewport) since the tooltip's position no longer dictates where the target needs to land. The one-line fix that actually mattered: the 'top' placement branch now clamps with `Math.max(safeTopPx + 16, top - tipHeight - 14)` instead of the old flat `Math.max(16, ...)` — this is the root-cause fix v7.86 had sidestepped by removing the flip instead of fixing the clamp. Mask/ring behavior (clip to whatever of the target is visible, no oversized-target special-casing) is unchanged from v7.86 |
| v7.86 | **Onboarding tour engine — tooltip now always pinned below the safe area instead of dynamically flipping above/below the target.** Replaced the old "prefer below the target, flip above if there's no room before the nav bar" placement logic (which could put the tooltip above the target near the top of the screen, rendering it underneath the status bar / dynamic island with its Back/Next buttons unreachable — reported live via screenshot) with a fixed placement: the tooltip always sits `safeTop + 12px` from the top of the viewport, full stop, for every step. `_renderTourStep()` now renders the tooltip's content first (to measure its real height), then scrolls the target down just far enough to land below the tooltip + arrow, using the same scroll-to-offset approach for every step regardless of target size — this also retires the separate v7.84 "oversized target" branch entirely, since there's no longer a top/bottom flip decision to get wrong for a tall card. The mask/ring simply clip to whatever portion of the target is currently in the viewport, with no attempt to guarantee the whole thing is visible — matches the explicit design call to let "the spotlight show whatever is visible on the screen" rather than engineering around every possible target size. Also dropped the now-unused `placement: 'auto'|'top'|'bottom'` step option, since nothing in the step lists used it and the engine no longer has a flip to override |
| v7.85 | **Onboarding tour engine — scroll-reset on every step, plus three Back-specific respotlight bugs.** `#content` (the app's only real scrollable element — html/body never scroll) now resets to `scrollTop: 0` at the top of every single `_renderTourStep()` call, Next and Back alike, before any measurement happens — fixes the tooltip rendering out of frame after the person manually scrolled mid-tour. Along the way, found that the tall-target scroll branch was using `window.scrollTo`/`window.scrollY`, which are dead code against `#content` (always a no-op, since `window` never scrolls in this layout) — switched to scrolling `#content` directly. Three Back-specific bugs fixed by giving the relevant steps' `onEnter` handlers the resets they were missing: (1) Progress's first insights-card step didn't collapse the AI-advice section or re-render on Back, so the ring stayed sized to whatever the narrative step had left it as; (2) the Aggressive-plan step didn't reset `insightsIndex` back to 0, so Back from the Next Cycle step (which jumps the swipe deck to index 2) left the deck showing the wrong card under the tooltip; (3) the Nutrition ellipsis-menu step didn't close `modal-nutr-manual`, so Back from the Quick Add step left that modal open over the spotlighted button. All three fixed in both the Demo Tour and their respective Mini-Tour copies |
| v7.84 | **Onboarding tour engine — two real bugs in Back navigation and oversized-target spotlighting, both fixed at the engine level.** (1) Every step's `showScreen(...)` call used to live inside individual `onEnter` handlers, and only the FIRST step of each page segment ever had one — correct for forward navigation (which always arrives at a segment via its first step) but broken for Back: landing on a step deep in a segment from a different page's step left the wrong screen active, and since inactive screens stay in the DOM (just hidden), `getBoundingClientRect()` on the target returned a zero rect pinned at (0,0) — exactly the "spotlight resolves to top-left corner" symptom reported. Fixed by centralizing screen management: every step across all tours now declares an explicit `screen` property, checked once in `_renderTourStep()` before `onEnter` runs, rather than each step managing its own navigation ad hoc. (2) The AI-advice narrative step's expanded card can be genuinely taller than the viewport (found live: 1497px on a 844px-tall screen) — centering a target that size via `scrollIntoView` left both the top and bottom mask pieces clamped to zero height, which looked exactly like the whole page was spotlighted rather than just the card. First attempt (`block:'start'`) didn't fix it either — that aligns the target flush with zero margin, so the top mask was still zero, just at a different scroll position. Fixed with an explicit scroll offset (target's top minus a fixed margin) for any target taller than 85% of the viewport, guaranteeing a real, visible dark border above the spotlight regardless of how tall the content below runs. Verified with Playwright: a full 17-step forward walk and a full 16-step backward walk (crossing every single page boundary in both directions) both produced valid, non-zero target rects at every step except the one already-known, already-flagged Dinner-recipe issue (unrelated, still pending that data file) |
| v7.83 | **Onboarding tour — Skip confirmation, AI-advice steps split into 3, and a previously-missing Next Cycle spotlight.** `tourSkip()` now shows a small confirmation overlay first (Cancel returns to the tour untouched, "Yes, skip" does what Skip used to do immediately) — its own dedicated overlay at z-index 410 rather than the app's generic `showConfirm()`/`modal-confirm`, since that sits at z-index 320, below the tour's own mask, and would render invisibly behind it. The Progress AI-advice walkthrough is now 3 steps instead of 2 — narrative, Sustainable plan, Aggressive plan (the last one explicitly calling out "Challenge this") — reading `state.blocAdvice.chosenPath` at each step's `onEnter` to decide which of the underlying `.selected`/`.alt` flags actually corresponds to which named plan, rather than assuming Aggressive is always the chosen one; correct regardless of which path a real Mini-Tour user (or a differently-configured dataset) actually picked. Added a genuinely missing step spotlighting card 3 of the insights deck — the Next Cycle Recommendation Engine and its own "ask BLOC for a second opinion" feature, which the tour had entirely skipped over despite being one of three cards in the same swipeable deck already being toured. Verified end-to-end with Playwright: Skip confirmation's Cancel/Confirm paths both behave correctly, the Sustainable/Aggressive steps correctly resolve against the demo dataset's actual `chosenPath` ('aggressive'), the Next Cycle card genuinely renders its real content when spotlighted, and the full (now 17-step) Demo Tour walks through to completion with no crashes or skips |
| v7.82 | **Onboarding tour — round of tweaks + two real bugs found and fixed.** Demo Tour now opens with a `modal-tour-welcome` screen ("Welcome to BLOC…") before the spotlight sequence begins, rather than jumping straight in — `startDemoTour()` opens it, its "Let's go →" button calls the new `beginDemoTourSteps()`. Added a Back button to the tour tooltip (wired to the `tourBack()` engine function that already existed but was never exposed in the UI), hidden on the first step; best-effort, not a true undo, for steps whose `onEnter` has a real side effect. Removed the swipe-to-copy-from-yesterday line from the Nutrition step's copy, leaving only the `···` menu (both Demo Tour and Mini-Tour). Retargeted the Progress "swipeable decks" step from the hero card — no longer a swipe deck since the flat-design redesign — to the weekly-summary deck (`progress-tables-wrap`), with copy referencing the insights card seen a few steps earlier instead. **Real bug #1**: the AI-advice narrative/plan steps set `_blocAdviceSectionsOpen.narrative`/`.selected`, but never touched `_aiAdviceBodyOpen` — a separate, outer collapse gate (the "View BLOC AI advice" toggle) wrapping the entire advice card body, so the inner sections stayed hidden regardless of their own flags. Fixed in both the Demo Tour and the Progress Mini-Tour; confirmed live that advice headline/narrative text now actually appears in the DOM. **Real bug #2**: `scrollIntoView({behavior:'smooth'})` could still be mid-animation when the tour engine's 220ms settle timeout fired for a large scroll distance, positioning the tooltip against a mid-scroll rect — reproduced live as a tooltip rendered ~150px below the viewport with its Next button completely unreachable, surfaced by the new Progress-tables-wrap target (a much bigger jump than any previous step needed). Fixed at the engine level (§33) by switching to instant scroll, removing the whole category of "did the animation finish in time" races for every tour, not just this one step. Also fixed a self-inflicted bug in the new welcome modal: used JS-style `\u2192`/`\u2014` escapes inside static HTML, where they don't get interpreted and render as literal backslash-u text — replaced with the real characters. Verified end-to-end with Playwright: full 15-step Demo Tour walk-through with zero skips or timeouts, Back button tested working, AI-advice fix confirmed via DOM inspection |
| v7.81 | **Onboarding tour system — Mini-Tours + API-key redirect logged (Build Order steps 8 & 9, core tour system complete).** New Help icon on the Settings screen opens a modal with five on-demand, manual, Settings-triggered Mini-Tours (Home/Progress/Plan/Train/Nutrition) — reuses the Demo Tour's per-page copy but points at the person's REAL current data, with no date anchor and `allowSkip: true` throughout (unlike the two forced tours, these are optional exploration the person opted into, so they always have an easy way out). Structured with a future Tours-vs-FAQs segmented control in mind per scope §8.4, even though only Tours ships now. Same leftover-modal problem as the Demo Tour bug two versions back turned up again in the Nutrition Mini-Tour (its last step opens `modal-nutr-manual` and nothing closed it) — fixed with `onComplete`/`onSkip` handlers rather than another blanket sweep, since a Mini-Tour (unlike the Demo Tour's wipe-and-handoff) has no natural point to sweep-close everything. Step 9 (the API-key-missing redirect on "Ask BLOC for advice") was explicitly scoped as a backlog item rather than a build task — logged in TECHNICAL.md's Known Limitations rather than implemented, per the scope doc's own framing. Verified with Playwright against synthetic real-data accounts (not `demoData`): Train Mini-Tour gracefully degrades when no exercise is locked or mid-progression; a genuinely empty account (0 macrocycles) completes the Home and Train Mini-Tours without error; Skip Tour ends a Mini-Tour cleanly. One unrelated pre-existing bug found and logged (not fixed, out of scope) while building test fixtures: `renderProgress()` crashes on a macrocycle with a goal type but zero body logs, reproducing with plain screen navigation and no tour involved |
| v7.80 | **Onboarding tour — Macrocycle Creation Tour (Build Order step 7 complete).** After the profile gate closes on a brand-new account, `startMacrocycleCreationTour()` walks it through creating a real first macrocycle — goal type, the Monday-only start-date rule, split/microcycles, then a genuine pause (`waitForAction` on the real "Create Macrocycle →" button — nothing is faked, the person fills in and submits the actual form), then Weight vs Cardio and exercise type inside the real Add Exercise modal, ending on a Link-exercises/supersets explainer. Has a Skip Tour exit throughout (unlike the Demo Tour), and skipping leaves the macro-creation modal open exactly as it was, so the person can keep filling it in unguided rather than losing their in-progress form. One copy correction: the scope doc's original Monday-rule tooltip said BLOC "snaps your start date forward automatically" — the app actually blocks the save and flashes the field red instead (per the v7.29 entry below), so the tooltip was rewritten to match real behavior rather than ship copy that would contradict what the person sees happen. One more real bug found and fixed: the Demo Tour's last step leaves `modal-nutr-manual` open by design (it ends on that step), and nothing was ever closing it — `exitDemoMode()` now sweep-closes every open modal before wiping state, so a leftover modal can never stack in front of the profile gate or this tour. Verified end-to-end with Playwright: a full run from Demo Tour completion through profile entry through a genuinely-created macrocycle (with the "Add a goal?" confirm dialog correctly auto-dismissed) to tour completion, plus a separate Skip Tour run confirming the macro-creation modal survives a skip. Still outstanding: Settings Help icon → Mini-Tours modal (step 8) and the API-key redirect (step 9) |
| v7.79 | **Onboarding tour — profile-entry hard gate (Build Order step 6 complete).** Reuses the existing Settings → About me profile modal rather than a new screen: `closeModal()` now refuses to dismiss it while gender/height/DOB are incomplete and the gate is active — blocking backdrop-tap, swipe-down, and the ✕ button all at once, since all three already funnel through that one function — and `saveBodyProfile()` now requires all three fields before a gated save is allowed through, with an inline validation message otherwise. `exitDemoMode()` now hands off into this gate directly. **`clearAllData()` now preserves profile** (previously wiped it, which contradicted this feature's own requirement that profile survive a data wipe). A real ordering bug surfaced during testing: a boot-time gate check for returning-but-empty accounts referenced `_tourState`, which is declared later in the same script — deferred with `setTimeout(..., 0)` to run after the rest of the script has finished executing. Verified with Playwright across 4 scenarios: Demo-Tour-to-gate handoff with full dismiss-blocking and validation; a returning device with 0 macrocycles gated on boot; an existing real account with macrocycles never gated even with an incomplete profile (preserves backward compatibility); Clear All Data preserving an already-complete profile |
| v7.78 | **Onboarding tour — Demo Tour sequence live (Build Order step 5 complete).** A genuinely new user (empty `bloc_state`) now sees the full 15-step Demo Tour automatically on first load: `demoData` loads into `state` (memory-only, never saved), the "today" anchor pins to 2 Aug 2026, and `startTour()` walks Home → Progress (including the two-step AI-advice narrative/plan expand from v3 §10.4) → Plan → Train → Nutrition, ending with a wipe-and-handoff back to a genuinely empty state and a cleared anchor. One real bug found and fixed along the way: the Train steps originally targeted a specific exercise by name (assuming Lat Pull Machine would render as the normal-progression example and Lat Pulldown as the locked one, per the scope doc), but live progression-lock evaluation can legitimately disagree with a dataset's own expectations about which exercise ends up locked — now both steps expand every exercise on the tour's landing day and dynamically find whichever one actually has a `prog-selector-`/`locked-notice-` element in the DOM, so the tour can never point at the wrong exercise regardless of what the live logic computes. Verified end-to-end with Playwright: all 15 steps advance with zero auto-skips, the anchor date and macro data are visible mid-tour, and the final state/anchor both correctly revert on completion. Still outstanding: the profile-entry hard-block gate (step 6) doesn't exist yet, so the tour currently hands off to a plain empty Home screen instead of forced profile entry |
| v7.77 | **Onboarding tour — "today" anchor mechanism (part of Build Order step 5, no tour entry/exit wiring yet).** New `now()` function is the single override-aware source of "what is today" throughout the app; `setTourAnchorDate('YYYY-MM-DD')` makes every caller see a fixed date instead of the device's real clock, `setTourAnchorDate(null)` reverts instantly. `getLocalToday()` now just calls `now()` — and a further 17 call sites that were independently constructing `new Date()` for "today" (Progress's date header, the weekly-average window, plateau detection's 14-day cutoff, day-of-week for the Home weekly-advice calc, `calcAge()` which feeds BMR/TDEE, cycle-active/past comparisons, ETA projections, the Nutrition date-picker's Yesterday button, and more) were audited one-by-one and converted to route through it too — these had all been silently bypassing `getLocalToday()` entirely despite being the exact same "today" concept. Two call sites deliberately left untouched: a new macrocycle's default name-year (should always reflect the real year, even mid-tour) and `now()`'s own internal fallback. `Date.now()` (epoch-ms uniqueness tokens for ids) is unaffected by design — anchoring those would risk collisions, not prevent them. Verified with Playwright: anchor propagates correctly to `getLocalToday()`, `calcAge()`, and day-of-week; clears cleanly back to the real device date; the real-year exception holds while anchored. Nothing in the app calls `setTourAnchorDate()` yet — that's the Demo Tour's actual enter/exit sequence, still to come |
| v7.76 | **Onboarding tour — demo-data fetch wired (Build Order step 4, no content yet).** `bloc-demo-data.json` now ships as a second static file alongside `index.html` — the app's first-ever network dependency (same-origin, not third-party). A new-user check (`bloc_state` absent from localStorage, tested before `load()` runs) triggers a one-time fetch on boot; a returning user's boot never requests the file at all. Fails completely silently on a slow connection, ad-blocker, or missing file — `demoData` just stays `null` and the app boots exactly as it always has, only a console warning logged. Verified with Playwright against a local static server: new-user fetch succeeds and populates `demoData`, returning-user boot makes zero request, and a deliberately-blocked fetch leaves the app fully functional with no uncaught error. Nothing consumes `demoData` yet — that's the Demo Tour sequence itself, Build Order step 5 |
| v7.75 | **Onboarding tour — spotlight/tooltip engine built (Build Order step 3, no content yet).** New shared `startTour(steps, opts)` engine (`TECHNICAL.md` §33) that the Demo Tour, Macrocycle Creation Tour, and all five Mini-Tours will call into. Four-div frame masking (not a clip-path hole) so action-required steps can be advanced by a real tap on the actual target element, with no synthetic event dispatch. Tooltip auto-flips above/below the target to stay clear of the floating nav bar, with a Skip-tour link (opt-in per tour), progress dots, and a `waitForAction` mode alongside the normal Next-button mode. Verified with a Playwright smoke test (rect/mask math, top/bottom flip, tap-to-advance, Skip-vs-Complete callback separation, full DOM teardown on both paths) — real iOS PWA verification still outstanding. Nothing user-facing yet; no tour actually starts from anywhere in the app until the Demo Tour content (step 5) is built |
| v7.74 | **Onboarding tour — prep pass 1/2 (no user-visible change).** Added stable `id` attributes to the last remaining spotlight-target elements ahead of the upcoming Demo/Macrocycle/Mini-Tour system: Train's per-exercise progression selector (`prog-selector-{macroId}-{exId}`) and locked/missed-target notice (`locked-notice-{macroId}-{exId}`); the deload toggle span (`deload-toggle-{macroId}`); the Nutrition "Fill day" button (`fill-day-btn`); and the per-meal ellipsis/options button (`meal-menu-btn-{meal}`). Purely additive — no logic touched. Five other originally-scoped targets (Ask BLOC for advice button, sample-day save-prompt banner, Per item/Per gram toggle, swipe-to-copy strip) turned out to already have stable IDs from earlier work, so didn't need changes |
| v7.69–v7.72 | **Home: "Edit today's logs" link removed**, superseded by the steps card's permanent "View logs" button (see v7.60–v7.68 below) — the link, its render function, its dedicated modal helper (`openTodaysBodyLogModal()`), and its CSS were all deleted rather than left dormant, along with a couple of stale in-code comments that still referenced it. Bundled bug fix: the steps card's "View logs" button had been gated on `!tracked`, so a brand-new account with zero logged data saw it duplicated across *every* metric card (kcal/protein/carbs too) — now keyed to `m.field === 'steps'` specifically, so it appears only on the steps card, and unconditionally (not just when untracked) |
| v7.60–v7.68 | **New: Cardio exercise support** (Run, Bike, Row, plus custom). *Data model*: exercises/library entries gain `category: 'weight' \| 'cardio'` (legacy entries default to `'weight'` via `getLibraryCategory()` — no migration needed); cardio exercises store `metricType` (time or distance as the plan-time target), `targetSeconds`/`targetDistance`, `distanceUnit` (km/m/mi), optional `speedLevel`/`resistanceLevel`, plus the normal `setsStart`/`setsEnd`. *Plan*: Weight/Cardio toggle reconfigures the whole exercise modal; cardio mode offers a Time-vs-Distance target toggle, time or distance input, a per-exercise km/m picker (shown only when the global unit preference is Km), and optional speed/resistance fields; the exercise picker filters to the active category so weight and cardio lists never mix, and custom exercises appear under whichever category they were created in (the "Add Custom" modal hides the now-irrelevant Body Part field for cardio); sets range widened to 1–15 (was 2–7) for every exercise type, dropping the separate "standalone" checkbox — a single-set session is just 1→1 sets; cardio exercises show a readable summary ("3–5 sets · 30s target · resistance 8") and a cardio badge, with superset members showing "follows leader" instead of their own irrelevant sets range. *Settings*: new "Distance Units" preference (Km/Metres vs Miles). *Train*: full logging card per cardio exercise — expand/collapse, grid-aligned column headers (Set / Target / logged unit / speed / resist / ✓), one row per set logging whichever metric isn't the fixed target, optional level inputs, done toggle; every input's placeholder is last week's logged value at that set index (no computed suggestion — cardio has no progression); header shows last-*logged* speed/resistance plus a "Last wk: +500m/−30s" delta badge; Fill Suggested only fills speed/resistance levels, Clear wipes logs as normal; fully exempt from progression suggestions, the Progression Compliance Guard, and deload effects; cardio can be a superset member alongside weight exercises, following the leader's set count, with its own per-row mini labels. *Bugs fixed*: sets dropdowns getting permanently stuck disabled after editing a non-leader superset member (missing reset on fresh Add); a superset's combined done-count badge always counting cardio members as 0 sets done; "min" vs "m" label ambiguity next to metre distances; cardio column headers misaligned with their data rows (switched to CSS grid, matching the technique weight exercises already used). *Unrelated tweaks bundled in this session*: nav bar changed to full screen width (was a floating auto-width pill) with all labels permanently visible (was active-tab-only); Progress's weekly summary table gained an "Avg weight" column, left of "Δ weight" |
| v7.65 | **Session covering 5 dev-notes items.** *Count-up animations*: Home, Progress, Train, and Nutrition hero numbers/bars now count up from 0 on an eased ~1000ms timeline, strictly on actual page load (never on in-page re-renders like a quick-log save or a set toggle) — required disabling the CSS `transition: width` on JS-driven bars to stop it fighting the animation loop (was causing a stall-then-snap artifact). Steps log box gained the same save-confirmation swipe animation as weight/measurements, but without the result-flash text (swipe, then vanish — no hold). *Home weekly advice reconciled*: kcal/protein/carbs "adjust by X" advice is no longer four independent linear catch-up calculations that can contradict each other (e.g. asking for less kcal and more protein simultaneously, when the extra protein alone costs more calories than the reduced budget allows) — now reconciled: protein's full catch-up is honoured only if it fits the kcal budget alongside carbs/fats at 50g/15g floors; otherwise protein's shortfall is capped at 15g under its own goal and kcal absorbs the rest, clearly labelled. Added "New target" + "Projected weekly avg if met" bullets per adjusted metric, a fats-only line, and a today-excluded footnote. *Recipe ingredient grams fixed*: adding a library/scanned food into a recipe no longer discards its real weight and per-gram rate (was hardcoding `grams: 1`); any ingredient predating the per-gram schema (pre-v6.x recipes) is now editable by weight via a safely-derived rate (`kcal ÷ grams`) instead of being misread as a servings count — fixes both bugs retroactively with no migration needed. *New: "Per gram" manual entry mode*, in both the Nutrition Quick Add and Recipe Builder manual modals — enter totals for a stated weight instead of a flat per-item amount; stores a real per-gram rate (editable later) and saves to the food library, for anything genuinely weighed that the scanner can't find |
| v7.64 | Home log save-confirmation animation hold time adjusted to 3s (settled value, after briefly trying 2s in v7.63) |
| v7.63 | Two fixes: the Nutrition dinner fill-day confirm prompt was rendering behind the food library add modal (which deliberately stays open during quick-add so multiple items can be logged in a row) — `modal-confirm` now always renders above every other modal tier. Accepting the prompt now also closes that modal before filling the day; declining still only closes the prompt itself, leaving the food library modal open. Bundled: log save-confirmation animation hold time trimmed 5s→2s (superseded by v7.64) |
| v7.62 | Two bug fixes: Home's measurements log box was missing its "Log measurements" header (present on the weigh-in and steps boxes but not measurements) — added to match. The Nutrition dinner Sample Day Library prompt was firing on every item logged into Dinner rather than only a qualifying one — it now only prompts when the logged item is a saved recipe whose name matches a dinner already stored in the library group linked to the day's goal, using the matched day rather than always the group's most recent one |
| v7.61 | New save-confirmation animation on Home's weigh-in and measurements log boxes (`playLogSaveAnimation()`): a lighter panel swipes in over the inputs/button, then a one-line-per-metric result flash shows the prior log and the delta against it, holding briefly before the box hides itself the normal way |
| v7.59–v7.60 | **Redesign refinement pass** — bug fixes and follow-on tweaks discovered after v7.58's cosmetic redesign, not a further redesign in itself. *Global*: true red (`--red: #E24B4A`) reinstated for danger/destructive states after it had been silently overwritten to a muted rose during the initial redesign pass; that muted tone was split out into its own `--warn` token for warning/drift visuals that still want a softer colour. Every pill-shaped element (tags, badges, progression chips, week pills, day tabs, hero nav-arrow buttons) changed from fully round to rounded-rectangle. All hero eyebrow labels forced uppercase via CSS. Every hero progress bar gained a dot marker at the end of its filled portion. *Home*: new "Averages vs target" page title; the four per-metric badges replaced with one consolidated, expandable status message giving per-metric catch-up advice; fixed a real pace-calculation bug that had been treating unlogged days as zero progress against a fixed 7-day target (wildly inflating the catch-up number whenever there were gap days) — only tracked-so-far days plus remaining days now count toward the target; badge classification made pace-aware instead of a naive average-vs-target check; protein/steps exceeding target now classify as plain "On track" rather than a separate "Exceeding" state; fixed a CSS bug where `:first-of-type` was silently never matching (it matches by tag, not class) — replaced with adjacent-sibling selectors so inter-row dividers actually render; measurements block's card wrapper stripped, and divider logic around the goal banner/log boxes cleaned up to exactly one divider between sections. *Progress*: hero swipe deck reduced from 5 slides to just the Active Cycle card — weight trend is now a collapsible sparkline on that same card, and the steps/kcal bar-chart visual moved into the Statistics card; cycle hero summary trimmed to goal/length/end-date; "falling behind" schedule-pace text changed from red to blue; Plateau/insights card consolidated into one bordered card, "View intake drift" removed entirely; added a missing "Weekly Summary" header above the table swipe deck; Weekly Macro Split's per-week card wrapper removed in favour of flat dividers; Statistics card gained a 7-Day/All toggle (All view: full-cycle horizontally-scrollable bar-per-day chart, defaulting to the most recent 4 weeks); fixed several colour bugs where "good" states were incorrectly purple instead of green, and carbs' over-target state (amber → red); fixed a layout bug in the all-time chart where the week-separator's padding was shrinking a bar column's content width, causing bars to visually overflow at every week boundary — fixed via week-block wrappers, which also centre each week label. *Plan*: added a missing divider between the hero and the Goals section; macrocycle summary replaced with 3 callouts (Goal periods / Sessions / Goal weight) matching Progress, plus the goal/weeks/end-date subtitle restored; goal target rows changed from single-line-wrap-with-tap-to-expand (which conflicted with tap-to-edit) to a static two-line layout; "+ New" changed pill → rounded-rect; spacing/colour fixed above "Set your exercises here" (was hardcoded purple). *Train*: page title removed, eyebrow condensed to one line; timer button circle → rounded-rect; "Session progress" + % promoted to the top callout with the progress bar directly beneath; 3 callouts added (Exercises / Sets done / Volume); "Mark as deload" changed from a bordered button to plain blue text; removed a double-divider above the session picker. *Nutrition*: Fill day/Quick add buttons pill → rounded-rect; kcal progress bar moved directly below the kcal/goal line (removing a duplicate text row and divider); divider added after the macro callouts; "Saved to library" changed from a filled pill badge to plain checkmark text; food entry bullet points removed, meal dividers now only appear between meals; "+ Log" changed from a bordered button to plain accent-coloured text; meal nutrition summary shortened from "P:90g C:12g F:2g" to "P90 C12 F2". Not yet done: restoring the motion animation on the insights swipe deck was never circled back to |
| v7.58 | **Full visual redesign — cosmetic only, no functional changes.** New flatter "Signal"-style design language app-wide: card wrappers removed from within pages in favour of a three-tier divider system (`.divider-hero`/`.divider`/`.divider-sm`), every pill-shaped element changed to rounded rectangles, new dark near-black background (`--bg: #161826`), muted purple accent (`--accent: #9184d9`), Inter-only typography carried over unchanged. See v7.59–v7.60 above for the bug-fix/refinement pass that followed |
| v7.55–v7.57 | **New: Progression Compliance Guard.** If you select weight (or reps) progression for an exercise but don't actually hit the suggested number on every set, the following week's suggestion freezes at that same missed target — instead of continuing to climb — until a session matches it exactly, at which point normal progression resumes from there. Whole-exercise, not per-set (any one set missing it locks the lot); drop-set drop portions are exempt (never a fixed target, always "to failure"); giant/pause sets are checked the same as any other exercise, against whichever route was actually selected. Deload and the single post-deload session always override it entirely — a lock quietly rides through both untouched and resumes control the moment normal weeks return. While locked: the weight/reps progression chips are replaced with a "Progression on hold" note showing the frozen numbers, the collapsed card shows a single frozen figure instead of "last / next", the header shows `· ⚠ locked` in place of the "Last wk" route, and the one-tap fill-and-complete shortcut is hidden (forcing a genuine manual log). A completed session that missed its target shows `✓ done · ⚠ missed target` on the collapsed card. Locked/unlocked state only ever changes starting the week *after* you view it — logging a new miss doesn't hide this week's chips, and clearing a lock doesn't reveal them until next week either; the week after a lock clears also suppresses the "Last wk: ↑ weight" badge, since that week wasn't a genuine new progression choice. A render-time catch-up sweep means compliance is evaluated retroactively the moment you view a later week — not only reactively at the exact moment you tick a box — with each week's target cached the first time it's checked so repeated re-evaluation can never silently drift onto a different number. Bundled fix: reps values are now sanitised at every write path (typed, filled, or quick-completed) so a stray literal "reps" suffix (a pre-existing free-text-field quirk, e.g. "60 reps" instead of "60") can never re-enter storage and desync from a computed target |
| v7.54 | **Numeric keyboard pass** — `inputmode` set on every relevant field across the app (66 fields) so the right keypad shows instead of the full qwerty keyboard: whole-number keypad for steps, reps, waist/hip whole-inch entry, barcode manual entry, height ft/in, and mesocycle count; decimal keypad for weight, kcal/macros, grams/servings, and cm measurements. Driven by a full field-by-field audit rather than a blanket rule, since a few fields (Train's reps field, notably) are `type="text"` rather than `type="number"` to allow non-numeric entries, and needed the same treatment without changing their underlying validation |
| v7.53 | **Home log boxes reinstated as inline "disappearing input" cards**, replacing the icon-triggered mini-modals from v7.31–v7.52 — weight/steps show a plain input daily until logged; measurements now runs on its own 4-day cycle (not daily) with a warning banner, staying visible until a fresh measurement resets the countdown. The three modals this replaced were removed. **Nutrition**: logging anything into Dinner now checks for a matching Sample Day Library entry and offers to fill the whole day from it (same mechanism as the Fill Day button), checked on every Dinner addition |
| v7.50–v7.52 | **Settings — full redesign, iterated live across three passes.** Body and Goals nav tabs removed entirely (folded into Settings and Plan respectively — see below). Final structure: a list of collapsible cards (Profile, Nutrition, Exercise, expanded by default; Danger Zone, collapsed by default) each showing their buttons directly inline, no nested cards or description text. Profile card: App preferences (Mode/Theme, moved out of their own top-level section), About me, Body logs (the old standalone Body screen, trimmed down to just its grouped log list — the weight chart, inline log-form, and measurement reminder were removed along with ~200 lines of now-dead computation that fed only them), Linked services → API Key (moved from the old "AI Advice" section). Nutrition card: My Recipes, Sample libraries, View/edit/export/import food library, Import recipe. Exercise card: view/edit/export/import. Modals opened from these cards stack rather than closing their parent (own z-index tier), except the three that lead into the pre-existing Food Library/Recipes/Exercise Library modal chains, which still close their parent first to avoid rendering behind their own children. Home's "Edit today's logs" now opens Settings → Body logs → today's specific entry directly, via a new `openTodaysBodyLogModal()` |
| v7.40–v7.41 | **Goals folded into Plan — no longer its own screen.** Plan gained a collapsible Goals section (same row styling as Progress's cycle-goals list) with its own "+ Build goals" button, swipe-to-delete, and a dismissible "add a goal?" nudge on saving a macrocycle with none linked. Delete button added to the goal modal itself. Progress's own goal list restricted to active + upcoming only (full history now lives on Plan). Session cards on Plan's Week-1 template now auto-collapse once they have at least one exercise (previously always expanded by default), resetting to that default each fresh visit to the tab; manually toggling, or opening add/edit-exercise for a session, overrides the default until the next visit |
| v7.31–v7.34 | **New Home screen — the app's default landing tab**, ahead of Progress. Weekly hero card (kcal/protein/carbs/steps vs. active goal, metric-specific badge polarity, adjustment sublabel that only shows when needed); upcoming-goal banner (fires only on a genuine target change within 6 days, comparing old vs. new); three quick-log icon tiles (weight/measure/steps) that disappear once logged; planned-food-today and next-session previews. Nav bar reordered to Home/Progress/Plan/Train/Nutrition/Settings |
| v7.30 | Settings: `clearAllData()` now also resets `state.sampleDays` — previously omitted, the same class of oversight as the historical `supersets`/`profile` gap (never crashed anything, since `load()`'s defensive default silently backfilled it, but "Clear all data" left the Sample Day Library quietly intact) |
| v7.30 | **Critical Sample Day Library fix.** `range.proteinMax` was stored as the real JS value `Infinity` ("no upper bound on protein") — `JSON.stringify()` silently turns `Infinity` into `null`, and since every save round-trips state through JSON, this corrupted almost immediately after the feature shipped, silently breaking every match check that depended on it (both the linked-goal pill display and Fill Day discovery). Diagnosed against a real exported backup rather than synthetic numbers. Fixed with a JSON-safe sentinel (`Number.MAX_SAFE_INTEGER`) going forward, defensive `null`/`undefined`-tolerant comparisons everywhere a range is checked, and a one-time migration on load that repairs any already-corrupted saved library |
| v7.29 | Sample Day Library: a goal can now show as "linked" — and be used for Fill Day — purely because its own targets fall within an existing library's tolerance band, even before anyone has saved a single day under it specifically. Previously, linking only ever happened retroactively, at the moment a day was actually saved under a new goal |
| v7.25–v7.28 | **Sample Day Library — new feature**, plus a Progress/Train/Plan pass built alongside it. *Nutrition*: save a day close to its own goal's targets (with a recipe logged for Dinner) into a library grouped by goal; Fill Day copies a saved day's meals onto any date; Settings → Profile → Nutrition Libraries manages every saved library (relabel/delete, linked-goal pills). *Progress*: the Phase 1 trend/plateau card moved from its own standalone card into the Insights deck's first card, directly below the weight header; new goal-period summary rows (green accent for the active goal, red + countdown subtitle for the next upcoming one) below Statistics; the weekly table stack split its Swing column out into its own third card, covering weight/kcal/steps/protein instead of weight alone, with thousand-separator formatting. *Train*: the hero card now shows the real calendar week date range for the selected mesocycle/microcycle. *Plan*: macrocycles can now only start on a Monday — blocked (not just warned) on both creation and edits, since deload-week and calendar-date math throughout the app assumes it |
| v7.22–v7.24 | **"Challenge this advice" — mid-cycle AI advice only, single-reply.** One challenge opportunity per advice response: sends the original prompt + prior response + your challenge text back to the model, which returns an acknowledgment, a fully revised Sustainable/Aggressive pair, and (new) an `isSignificantRevision` flag. Diff view pairs each original step against its revised counterpart (added/removed steps called out separately) for both plans. Decline discards the revision entirely — the challenge affordance doesn't reappear on that advice instance either way. Accept branches on significance: a minor correction keeps the original headline/narrative in place with the acknowledgment shown as an italic subtitle; a significant one (the model's own read of the situation genuinely changed) replaces the headline/narrative outright rather than leaving stale reasoning next to a very different plan. `state.blocAdvice.priorAdviceThisCycle` now accumulates a condensed history of every check-in this cycle — including original-vs-revised numbers and the acknowledgment for any accepted challenge — so a later ask is never blind to what's already been tried. Unrelated bundled fix: the Next Cycle second-opinion's "available within 3 weeks" line is now a live countdown to that 3-week eligibility mark (weeks, then days under 2 weeks out) instead of a static message |
| v7.20–v7.21 | **Next Cycle — Phase 4 (LLM second opinion) shipped.** New prompt + API call, separate from the mid-cycle "Ask BLOC for advice" feature — its own `state.nextCycleAdvice` slot, its own 3-week-from-cycle-end gate, no shared cooldown. Schema returns a variable-length `plans[]` array rather than a fixed sustainable/aggressive pair: one plan normally; two, keyed to each side of an unresolved deadline conflict; or two — "sustainable"/"aggressive" — when no deadline was given for a cut or bulk, with a hard client-side check preventing a bulk's aggressive variant from ever running shorter than its sustainable one. Every plan must self-declare (or inherit) its exact intended length and fill it precisely — a mismatch is rejected outright, not silently accepted. Daily steps are only ever requested from the model during a cut; bulk/maintenance goals are force-set to 8,000 flat afterward regardless of what came back. Accepted plans route through the exact same macro-creation + goal-queue pipeline as the deterministic path. **v7.21** additionally teaches the deterministic engine to compute a "normal logic" alternative whenever it recommends simply extending the current cycle (continuing a cut → the maintenance bridge it would have recommended instead; continuing a bulk → the cut) — shown as a secondary option with its own build button, and fed to the LLM call as explicit context so its narrative weighs extending vs switching directly rather than only describing the switch in isolation |
| v7.05–v7.19 | **Next Cycle Recommendation Engine — Phases 1–3 shipped.** Phase 1: deterministic duration×depth matrix recommending a maintenance bridge after a cut (or continuing the cut, if it was short and mild) or a cut after a bulk (or continuing the bulk, if it was short with a mild rate/surplus); reverse-diet and step-and-hold direction ramps anchored to the current cycle's last planned goal rather than recent actual intake; "Build this plan next" launches the existing goal-queue flow with a macrocycle-creation step prepended. Phase 2: optional target-weight/deadline input, checked against a sustainable-weight floor/ceiling (hard boundary) and a 1.5%→1%/0.5% taper curve (pace boundary), with two one-tap alternatives offered on a pace conflict. Phase 3: mid-bridge TDEE-discrepancy recalibration flag surfaced on the existing trend-insights card, comparing a locally-scoped implied TDEE against the bridge's planned target over the most recent stable weeks |
| v7.00 | **AI Advice — Phase 1 (deterministic) + Phase 2 (LLM).** Progress: new trend insights card below the Insights deck — goalType-branched signal detection (plateau-creep, drift-warning, gain-deficit, etc.), per-week caloric drift visualisation, plateau detection at ≥0.5 lbs/week threshold. Historical rollup archives completed macrocycles (avgKcal/protein/carbs, startBw/endBw, waist/hip, plateau weeks, signals) capped at 10 cycles. AI advice card with Anthropic API integration (direct browser access via `anthropic-dangerous-direct-browser-access` header, model `claude-sonnet-4-6`, max 8,000 tokens): eligibility gates on intervention-warranting signals only; safety floor from log-based BMR × 0.80; protein minimum at 1g/lb bodyweight; partial-week flagging in prompt; 2-week cooldown from call date. Advice card has collapsible narrative, pre/post-choice plan layout (selected plan visible, unchosen plan collapsed). **Goal queue flow**: "Use plan" launches sequential modal — truncation step (edit-mode with explicit field override), LLM goal steps (new-goal mode), `_goalQueueAdvancing` flag prevents closeModal from aborting queue between steps, `_blocLabel` written to saved goals for reliable date cross-referencing. **Plan**: Maintenance added as third goalType — no load/reps progression suggested, progression selector hidden, last-week badge suppressed, set-count spacing and deloads unchanged. Bodyweight-relative pace warning on target BW field (1%/week loss, 0.5%/week gain ceiling). Schedule-status pill now has maintenance branch (weight stability ±2 lbs). **Body**: measurement reminder pill below weight chart — appears after 6 days without a waist/hip log, taps to open body log modal. API key stored separately in `localStorage` (`bloc_api_key`), never exported in state backups |
| v6.12 | Body: new waist/hip measurement tracking (inches/cm input toggle, hero callouts, weekly deltas). Progress: weekly summary and measurements tables merged into a single swipeable dot-paginated stack; weight delta recalculated as this-week-average vs last-week-average instead of a single-day-to-single-day comparison, and a new Swing column shows the week's smallest/largest day-to-day change. Nutrition: recipe builder's "Add ingredients" screen gained a Food Library search option, reusing the same search modal as meal logging but routed to add ingredients instead of log a meal, including a modal z-index fix so it correctly layers above the recipe builder screen it's opened from |
| v6.11 | Body: Recent Entries now groups any month's completed logs (weight + steps both filled in) into a collapsible "Mmm YYYY" summary, sorted newest first; incomplete entries always stay visible individually regardless of month, so a steps-sync lag never buries an entry you still need to finish |
| v6.10 | Settings: consolidated Exercise + Food library management into one "Libraries" section with a consistent two-tier button layout; new in-app **Exercise Library Editor** (view/edit/delete custom exercises; built-ins read-only); Danger Zone reorganised — Restore-from-backup moved in alongside Clear-all-data, the latter now styled with a solid red fill matching Export's visual weight |
| v6.09 | Train: progression suggestions are now computed **per set** (from that same set number's previous mesocycle, not uniformly from set 1) instead of one flat figure for the whole exercise — a newly-added set inherits the previous week's final set. Fill Suggested, quick-fill, and quick-fill-complete all updated to match. Drop Set exercises gained a real main-set reps target (previously none at all) — the drop portion still has no target and is still discovered live. Quick-fill-complete is now available for drop sets too (previously no shortcut existed for them). As a side effect, drop sets' main set now contributes to the Plan page's theoretical volume projections |
| v6.08 | Train: one-tap "quick fill + complete" now also records exercise history, matching the checkbox path (deload weeks still excluded either way) |
| v6.07 | Exercise history recording excludes deload weeks, so a reduced week can never become the reference for "last logged" or a new plan's defaults |
| v6.06 | New **exercise history** — completing a set remembers weight/reps/tracking-mode per exercise + set type, powering a "Last logged" reference and auto-filled defaults in the Plan exercise modal. Set-type rename, at the data level: Myorep Giant Set → Giant Set, Myorep Matching → Pause Set. Every stored `type` value, function, and variable renamed to match, not just labels |
| v6.05 | New **Drop Set** exercise type — main weight planned, everything else logged to failure live; one Main/Drop row pair per set in Train |
| v6.04 | Plan: "sessions per week" is now calculated live from your split instead of typed in separately |
| v6.03 | Train: day-tab pills grouped by real calendar week instead of by microcycle; deload status pill text tightened |
| v6.02 | Deload: fixed reversion logic for the session(s) immediately following a deload on a 2-week mesocycle; overall progress badge and "not a deload week" placeholder text cleaned up |
| v6.01 | New **deload weeks** — mark a week/microcycle as deload, all weights reduce to 60% with automatic reversion the following session. Weight increment field generalised to any goal type (was loss-cycles-only) |
| v6.00 | Inline comments added throughout entire code base |
| v5.44 | Settings: "Clear all data" now preserves theme/mode and resets every field `load()` expects (previously left `state.supersets` undefined, a live crash risk) |
| v5.42–5.43 | Goals: overlap prevention between goal periods; active/past goals no longer silently drift their macros from bodyweight changes; required-field validation on kcal/steps |
| v5.41 | Nutrition: removed several functions and one dead code branch left over from earlier redesigns (old day-badge strip, an unused macro-view toggle, an unused recipe-builder entry point) |
| v5.33–5.39 | Plan: body part cycle volume table, session rename/collapse, progression preview redesigned to group by session instead of week. Train: last-week progression data and badges reorganised into the header/set rows; several bug fixes (per-side volume calc, badge inference logic, superset row alignment) |
| v5.16–5.32 | Progress: weight chart rebuilt to anchor on the live active cycle with a day-index-aligned comparison overlay; macrocycle state management overhaul; superset/tri-set feature fully implemented |
| v5.08 | iOS standalone viewport fixed via `measureEnv()` DOM probe + `--app-height`; nine themes with per-page hero colours; dark/light mode toggle; floating pill nav; Progress screen with 5-slide swipeable hero deck |
| v3.08 | Swipe-left gestures on all list rows, tap-to-edit, yesterday strip, edit modal defaults grams to RSS |
| v3.03 | Camera barcode scanning (BarcodeDetector + ZXing fallback), auto-start camera, manual edit mode |
| v3.01 | Meal ellipsis menu, swipe-to-copy from yesterday, recipe builder from nutrition log, SVG icons, Fill Suggested animations |
| v2.12 | Recipe builder, food library editor, rest timer, food library export/import |
| v2.10 | Home page overhaul — Today/7-day toggle with animated bar charts |
| v2.09 | Full nutrition page rebuild — date picker, day badges, kcal summary, macro panel, barcode/manual/library/quick-add |

---

## License

MIT — personal use, no warranty.
